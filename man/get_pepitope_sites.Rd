% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pepitope.R
\name{get_pepitope_sites}
\alias{get_pepitope_sites}
\title{Retrieve Epitope Residue Sites for Influenza HA Subtypes}
\usage{
get_pepitope_sites(
  subtype,
  sites = c("a", "b", "c", "d", "e"),
  harmonize_b_lineages = TRUE,
  seq_includes_signal_peptide = TRUE
)
}
\arguments{
\item{subtype}{Character. HA subtype or lineage, e.g., `"H1N1"`, `"A(H3N2)"`,
`"B/Victoria"`, etc.}

\item{sites}{Character vector. Which epitope sites to return. Must be a subset of
`c("a", "b", "c", "d", "e", "all_epitopes")`.
Defaults to `c("a", "b", "c", "d", "e")`.}

\item{harmonize_b_lineages}{Logical. If `TRUE`, merges all B lineages into a
unified "B" category. Defaults to `TRUE`.}

\item{seq_includes_signal_peptide}{Logical. If `TRUE`, epitope sites will be
offset by the length of the HA signal peptide. Defaults to `TRUE`.}
}
\value{
A named list of integer vectors, with names corresponding to the
  requested `sites`.
}
\description{
This function returns epitope residue positions (sites A–E and optionally all
sites combined) for a given influenza HA subtype. It supports H1N1, H3N2,
and influenza B lineages (Victoria, Yamagata, or pre-split) with options for
harmonizing B lineages and adjusting for signal peptide inclusion.
}
\examples{
get_pepitope_sites("A(H1N1)", sites = c("a", "b"))
get_pepitope_sites("B/Yamagata", harmonize_b_lineages = FALSE)

}
